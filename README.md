# CalloutLab

Callouts y exámenes para el equipo. Los datos (sesión, callouts por mapa, perfiles, resultados de exámenes) se guardan en el navegador y persisten entre sesiones.

```bash
npm install
npm run dev
```
